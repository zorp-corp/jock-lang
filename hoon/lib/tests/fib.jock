// fibonacci

// !=(=/(n 5 |.(?:(=(0 n) 1 ?:(=(1 n) 1 (add $(n (sub n 1)) $(n (sub n 2))))))))

// let n = 5;
// loop;
// if 0 == n {
//   1
// } else if 1 == n {
//   1
// } else {
//   $(n - 1) //+ $(n - 2)
//   // fib(n - 1) + fib(n - 2)
// }

func fib(n:@) -> @{
  if n == 0 {
    1
  } else if n == 1 {
    1
  } else {
    $(n - 1) //+ $(n - 2)
    // &1
  }
};

(
  // fib(0)
  // fib(1)
  fib(2)
  // fib(3)
  // fib(4)
  // fib(5)
  // fib(6)
  // fib(7)
  // fib(8)
  // fib(9)
  // fib(10)
)
