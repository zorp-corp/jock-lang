compose
  class Point(x:@) {
    add(p:(x:@ y:@)) -> Point {
      x + p.x
    }
  }
; // end compose

let origin = Point(50);
origin.add(70 80)

/*
!=
=+
|_  x=@
++  add
  |=  p=[x=@ y=@]
  (^add x x.p)
--
::~(. - 50)
=/  origin  ~(. + 50)
(add:origin [70 80])
*/
